column_name,data_type (PostgreSQL),constraints,description
complain_id,BIGINT,PRIMARY KEY; FOREIGN KEY → COMPLAIN(complain_id) ON DELETE CASCADE,민원 ID(1:0..1)
password_hash,VARCHAR(255),NOT NULL,비공개 열람용 패스워드 해시(평문 금지)
(rule),,visibility='private' 인 민원만 secret 존재 [권장: 트리거/RLS],부분 제약은 일반 CHECK만으로 강제 어려움